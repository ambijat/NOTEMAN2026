<Window x:Class="Noteman.Desktop.PromptEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add User Prompt" Width="560" Height="460"
        MinWidth="440" MinHeight="340" WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Prompt name" FontWeight="SemiBold" />
        <TextBox x:Name="TitleBox" Grid.Row="1" Margin="0,5,0,12" />
        <TextBlock Grid.Row="2" Text="Prompt text" FontWeight="SemiBold" />
        <TextBox x:Name="BodyBox" Grid.Row="3" Margin="0,5,0,10"
                 AcceptsReturn="True" AcceptsTab="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" />
        <CheckBox x:Name="PersistentBox" Grid.Row="4" IsChecked="True"
                  Content="Keep after closing NoteMan" Margin="0,0,0,12" />
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" IsCancel="True" Width="90" Margin="0,0,8,0" />
            <Button Content="Add Prompt" IsDefault="True" Width="100" Click="Add_Click" />
        </StackPanel>
    </Grid>
</Window>
