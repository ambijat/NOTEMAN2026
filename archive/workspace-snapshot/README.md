# Pre-consolidation workspace snapshot

These files were copied unchanged from the root of the local `NOTEMAN2026`
workspace during repository consolidation. They are historical evidence, not
the current application entry point.

Use `apps/ubuntu-python/` for the maintained Python application and
`apps/windows-dotnet/` for the maintained Windows application.
